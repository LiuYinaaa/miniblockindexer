import type { Transfer } from '../types/transfer.js';

export async function listTransfers(): Promise<Transfer[]> {
  // TODO: call transfer repository and map records to API response.
  return [];
}
